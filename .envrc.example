# Environment variables for Cloudflare Worker deployment
# Copy this file to .envrc and update with your actual values
# 
# Setup:
# 1. Install direnv: brew install direnv (macOS) or apt install direnv (Ubuntu)
# 2. Add to your shell: echo 'eval "$(direnv hook bash)"' >> ~/.bashrc
# 3. Copy: cp .envrc.example .envrc
# 4. Update the values below with your actual IDs
# 5. Allow: direnv allow

# Cloudflare Infrastructure IDs (get these from wrangler commands)
export KV_NAMESPACE_ID="your-kv-namespace-id"
export D1_DATABASE_ID="your-d1-database-id"

# Your Cloudflare Access and Admin Domains
export TEAM_DOMAIN="your-company.cloudflareaccess.com"
export ADMIN_DOMAIN="training-status.your-company.com"

# Optional: Set these if you want to override defaults
# export DEBUG="true"